#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "l do not fear computers. l fear the lack of them - lsaac Asimov";
	print_rev(str);
	return (0);
}
