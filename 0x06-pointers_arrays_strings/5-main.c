#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int mai(void)
{
	char s[] = "Lookup!\n";
	char *p;

	p = string_toupper(str);
	printf("%s", ptr);
	printf("%s", str);
	return (0);
}
